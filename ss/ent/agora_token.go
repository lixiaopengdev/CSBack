// Code generated by ent, DO NOT EDIT.

package ent

import (
	"CSBackendTmp/ent/agora_token"
	"CSBackendTmp/ent/user"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// Agora_token is the model entity for the Agora_token schema.
type Agora_token struct {
	config `json:"-"`
	// ID of the ent.
	// id
	ID uint64 `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// 声网token
	Token string `json:"token,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uint64 `json:"user_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the Agora_tokenQuery when eager-loading is set.
	Edges Agora_tokenEdges `json:"edges"`
}

// Agora_tokenEdges holds the relations/edges for other nodes in the graph.
type Agora_tokenEdges struct {
	// Owner holds the value of the owner edge.
	Owner *User `json:"owner,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e Agora_tokenEdges) OwnerOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.Owner == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.Owner, nil
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Agora_token) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case agora_token.FieldID, agora_token.FieldUserID:
			values[i] = new(sql.NullInt64)
		case agora_token.FieldToken:
			values[i] = new(sql.NullString)
		case agora_token.FieldCreateTime, agora_token.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Agora_token", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Agora_token fields.
func (at *Agora_token) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case agora_token.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			at.ID = uint64(value.Int64)
		case agora_token.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				at.CreateTime = value.Time
			}
		case agora_token.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				at.UpdateTime = value.Time
			}
		case agora_token.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				at.Token = value.String
			}
		case agora_token.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				at.UserID = uint64(value.Int64)
			}
		}
	}
	return nil
}

// QueryOwner queries the "owner" edge of the Agora_token entity.
func (at *Agora_token) QueryOwner() *UserQuery {
	return (&Agora_tokenClient{config: at.config}).QueryOwner(at)
}

// Update returns a builder for updating this Agora_token.
// Note that you need to call Agora_token.Unwrap() before calling this method if this Agora_token
// was returned from a transaction, and the transaction was committed or rolled back.
func (at *Agora_token) Update() *AgoraTokenUpdateOne {
	return (&Agora_tokenClient{config: at.config}).UpdateOne(at)
}

// Unwrap unwraps the Agora_token entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (at *Agora_token) Unwrap() *Agora_token {
	_tx, ok := at.config.driver.(*txDriver)
	if !ok {
		panic("ent: Agora_token is not a transactional entity")
	}
	at.config.driver = _tx.drv
	return at
}

// String implements the fmt.Stringer.
func (at *Agora_token) String() string {
	var builder strings.Builder
	builder.WriteString("Agora_token(")
	builder.WriteString(fmt.Sprintf("id=%v, ", at.ID))
	builder.WriteString("create_time=")
	builder.WriteString(at.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(at.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("token=")
	builder.WriteString(at.Token)
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", at.UserID))
	builder.WriteByte(')')
	return builder.String()
}

// Agora_tokens is a parsable slice of Agora_token.
type Agora_tokens []*Agora_token

func (at Agora_tokens) config(cfg config) {
	for _i := range at {
		at[_i].config = cfg
	}
}
