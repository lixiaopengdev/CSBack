// Code generated by ent, DO NOT EDIT.

package ent

import (
	"CSBackendTmp/ent/invite_code"
	"CSBackendTmp/ent/predicate"
	"CSBackendTmp/ent/user"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// InviteCodeQuery is the builder for querying Invite_Code entities.
type InviteCodeQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.Invite_Code
	withOwner  *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InviteCodeQuery builder.
func (icq *InviteCodeQuery) Where(ps ...predicate.Invite_Code) *InviteCodeQuery {
	icq.predicates = append(icq.predicates, ps...)
	return icq
}

// Limit adds a limit step to the query.
func (icq *InviteCodeQuery) Limit(limit int) *InviteCodeQuery {
	icq.limit = &limit
	return icq
}

// Offset adds an offset step to the query.
func (icq *InviteCodeQuery) Offset(offset int) *InviteCodeQuery {
	icq.offset = &offset
	return icq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (icq *InviteCodeQuery) Unique(unique bool) *InviteCodeQuery {
	icq.unique = &unique
	return icq
}

// Order adds an order step to the query.
func (icq *InviteCodeQuery) Order(o ...OrderFunc) *InviteCodeQuery {
	icq.order = append(icq.order, o...)
	return icq
}

// QueryOwner chains the current query on the "owner" edge.
func (icq *InviteCodeQuery) QueryOwner() *UserQuery {
	query := &UserQuery{config: icq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := icq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := icq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(invite_code.Table, invite_code.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, invite_code.OwnerTable, invite_code.OwnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(icq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Invite_Code entity from the query.
// Returns a *NotFoundError when no Invite_Code was found.
func (icq *InviteCodeQuery) First(ctx context.Context) (*Invite_Code, error) {
	nodes, err := icq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{invite_code.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (icq *InviteCodeQuery) FirstX(ctx context.Context) *Invite_Code {
	node, err := icq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Invite_Code ID from the query.
// Returns a *NotFoundError when no Invite_Code ID was found.
func (icq *InviteCodeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = icq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{invite_code.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (icq *InviteCodeQuery) FirstIDX(ctx context.Context) int {
	id, err := icq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Invite_Code entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Invite_Code entity is found.
// Returns a *NotFoundError when no Invite_Code entities are found.
func (icq *InviteCodeQuery) Only(ctx context.Context) (*Invite_Code, error) {
	nodes, err := icq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{invite_code.Label}
	default:
		return nil, &NotSingularError{invite_code.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (icq *InviteCodeQuery) OnlyX(ctx context.Context) *Invite_Code {
	node, err := icq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Invite_Code ID in the query.
// Returns a *NotSingularError when more than one Invite_Code ID is found.
// Returns a *NotFoundError when no entities are found.
func (icq *InviteCodeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = icq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{invite_code.Label}
	default:
		err = &NotSingularError{invite_code.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (icq *InviteCodeQuery) OnlyIDX(ctx context.Context) int {
	id, err := icq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Invite_Codes.
func (icq *InviteCodeQuery) All(ctx context.Context) ([]*Invite_Code, error) {
	if err := icq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return icq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (icq *InviteCodeQuery) AllX(ctx context.Context) []*Invite_Code {
	nodes, err := icq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Invite_Code IDs.
func (icq *InviteCodeQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := icq.Select(invite_code.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (icq *InviteCodeQuery) IDsX(ctx context.Context) []int {
	ids, err := icq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (icq *InviteCodeQuery) Count(ctx context.Context) (int, error) {
	if err := icq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return icq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (icq *InviteCodeQuery) CountX(ctx context.Context) int {
	count, err := icq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (icq *InviteCodeQuery) Exist(ctx context.Context) (bool, error) {
	if err := icq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return icq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (icq *InviteCodeQuery) ExistX(ctx context.Context) bool {
	exist, err := icq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InviteCodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (icq *InviteCodeQuery) Clone() *InviteCodeQuery {
	if icq == nil {
		return nil
	}
	return &InviteCodeQuery{
		config:     icq.config,
		limit:      icq.limit,
		offset:     icq.offset,
		order:      append([]OrderFunc{}, icq.order...),
		predicates: append([]predicate.Invite_Code{}, icq.predicates...),
		withOwner:  icq.withOwner.Clone(),
		// clone intermediate query.
		sql:    icq.sql.Clone(),
		path:   icq.path,
		unique: icq.unique,
	}
}

// WithOwner tells the query-builder to eager-load the nodes that are connected to
// the "owner" edge. The optional arguments are used to configure the query builder of the edge.
func (icq *InviteCodeQuery) WithOwner(opts ...func(*UserQuery)) *InviteCodeQuery {
	query := &UserQuery{config: icq.config}
	for _, opt := range opts {
		opt(query)
	}
	icq.withOwner = query
	return icq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.InviteCode.Query().
//		GroupBy(invite_code.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (icq *InviteCodeQuery) GroupBy(field string, fields ...string) *InviteCodeGroupBy {
	grbuild := &InviteCodeGroupBy{config: icq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := icq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return icq.sqlQuery(ctx), nil
	}
	grbuild.label = invite_code.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.InviteCode.Query().
//		Select(invite_code.FieldCreateTime).
//		Scan(ctx, &v)
func (icq *InviteCodeQuery) Select(fields ...string) *InviteCodeSelect {
	icq.fields = append(icq.fields, fields...)
	selbuild := &InviteCodeSelect{InviteCodeQuery: icq}
	selbuild.label = invite_code.Label
	selbuild.flds, selbuild.scan = &icq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a InviteCodeSelect configured with the given aggregations.
func (icq *InviteCodeQuery) Aggregate(fns ...AggregateFunc) *InviteCodeSelect {
	return icq.Select().Aggregate(fns...)
}

func (icq *InviteCodeQuery) prepareQuery(ctx context.Context) error {
	for _, f := range icq.fields {
		if !invite_code.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if icq.path != nil {
		prev, err := icq.path(ctx)
		if err != nil {
			return err
		}
		icq.sql = prev
	}
	return nil
}

func (icq *InviteCodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Invite_Code, error) {
	var (
		nodes       = []*Invite_Code{}
		_spec       = icq.querySpec()
		loadedTypes = [1]bool{
			icq.withOwner != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Invite_Code).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Invite_Code{config: icq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, icq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := icq.withOwner; query != nil {
		if err := icq.loadOwner(ctx, query, nodes, nil,
			func(n *Invite_Code, e *User) { n.Edges.Owner = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (icq *InviteCodeQuery) loadOwner(ctx context.Context, query *UserQuery, nodes []*Invite_Code, init func(*Invite_Code), assign func(*Invite_Code, *User)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*Invite_Code)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (icq *InviteCodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := icq.querySpec()
	_spec.Node.Columns = icq.fields
	if len(icq.fields) > 0 {
		_spec.Unique = icq.unique != nil && *icq.unique
	}
	return sqlgraph.CountNodes(ctx, icq.driver, _spec)
}

func (icq *InviteCodeQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := icq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (icq *InviteCodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   invite_code.Table,
			Columns: invite_code.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: invite_code.FieldID,
			},
		},
		From:   icq.sql,
		Unique: true,
	}
	if unique := icq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := icq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, invite_code.FieldID)
		for i := range fields {
			if fields[i] != invite_code.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := icq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := icq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := icq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := icq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (icq *InviteCodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(icq.driver.Dialect())
	t1 := builder.Table(invite_code.Table)
	columns := icq.fields
	if len(columns) == 0 {
		columns = invite_code.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if icq.sql != nil {
		selector = icq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if icq.unique != nil && *icq.unique {
		selector.Distinct()
	}
	for _, p := range icq.predicates {
		p(selector)
	}
	for _, p := range icq.order {
		p(selector)
	}
	if offset := icq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := icq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// InviteCodeGroupBy is the group-by builder for Invite_Code entities.
type InviteCodeGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (icgb *InviteCodeGroupBy) Aggregate(fns ...AggregateFunc) *InviteCodeGroupBy {
	icgb.fns = append(icgb.fns, fns...)
	return icgb
}

// Scan applies the group-by query and scans the result into the given value.
func (icgb *InviteCodeGroupBy) Scan(ctx context.Context, v any) error {
	query, err := icgb.path(ctx)
	if err != nil {
		return err
	}
	icgb.sql = query
	return icgb.sqlScan(ctx, v)
}

func (icgb *InviteCodeGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range icgb.fields {
		if !invite_code.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := icgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := icgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (icgb *InviteCodeGroupBy) sqlQuery() *sql.Selector {
	selector := icgb.sql.Select()
	aggregation := make([]string, 0, len(icgb.fns))
	for _, fn := range icgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(icgb.fields)+len(icgb.fns))
		for _, f := range icgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(icgb.fields...)...)
}

// InviteCodeSelect is the builder for selecting fields of InviteCode entities.
type InviteCodeSelect struct {
	*InviteCodeQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ics *InviteCodeSelect) Aggregate(fns ...AggregateFunc) *InviteCodeSelect {
	ics.fns = append(ics.fns, fns...)
	return ics
}

// Scan applies the selector query and scans the result into the given value.
func (ics *InviteCodeSelect) Scan(ctx context.Context, v any) error {
	if err := ics.prepareQuery(ctx); err != nil {
		return err
	}
	ics.sql = ics.InviteCodeQuery.sqlQuery(ctx)
	return ics.sqlScan(ctx, v)
}

func (ics *InviteCodeSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(ics.fns))
	for _, fn := range ics.fns {
		aggregation = append(aggregation, fn(ics.sql))
	}
	switch n := len(*ics.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		ics.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		ics.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := ics.sql.Query()
	if err := ics.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
